### Bit Magic

- Setting a bit

  ```c
  number |= 1 << x;
  // this will set the xth bit of number
  ```
  
- Clearing a bit

  ```c
  number &= ~(1 << x);
  ```

- Toggling a bit

  ```c
  number ^= 1<<x;
  ```

  
